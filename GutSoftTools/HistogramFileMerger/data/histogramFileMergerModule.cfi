module histogramFileMergerModule = HistogramFileMergerModule
{
  untracked string  OutputFileName               = "output.root"  
  untracked vstring InputFileNames               = { "input.root" }  
  untracked bool    DeleteInputFiles             = false
}
