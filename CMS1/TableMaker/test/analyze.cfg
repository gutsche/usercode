process A = {

  source = PoolSource
  {
    untracked vstring fileNames = 
    {
      "file:/uscmst1b_scratch/lpc1/cms1/jribnik/zmumu/1000/zmumu_reco100_1000.root"
    }
    untracked int32  maxEvents  = -1
    untracked uint32 skipEvents = 0
  }

  # cms1 table maker
  include "CMS1/TableMaker/data/tableMaker.cff"

  path p = {
    tableMaker
  }

}
